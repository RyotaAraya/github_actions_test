name: Git Tag
on:
 pull_request:
   branches:
     - main
   types:
     - closed
jobs:
 main:
   runs-on: ubuntu-latest
   if: github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'release-')
   steps:
     - uses: actions/checkout@v2
     - run: git fetch origin ${{ github.event.pull_request.head.base }}
     - run: git checkout ${{ github.event.pull_request.head.base }}
     - run: git tag `echo '${{ github.event.pull_request.head.base }}' | sed 's/release-/v/'`
     - run: git push origin `echo '${{ github.event.pull_request.head.base }}' | sed 's/release-/v/'`
